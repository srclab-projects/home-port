plugins {
  id 'build.jar'
}

description = "Ship Starter for Core: Core and basic interfaces, functions and utilities for Spring Boot."

java {
  registerFeature('optionalSupport') {
    usingSourceSet(sourceSets.main)
  }
}

dependencies {
  api project(":ship-core")
}