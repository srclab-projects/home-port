plugins {
  id 'build.jar'
}

description = "Ship Starter for Web: Web core and basic interfaces, functions and utilities for Spring Boot."

java {
  registerFeature('optionalSupport') {
    usingSourceSet(sourceSets.main)
  }
}

dependencies {
  api project(":ship-web")
}